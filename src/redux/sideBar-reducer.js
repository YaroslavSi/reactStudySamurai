let initialState = {
    friendsName: [
        {name: 'Andrey'},
        {name: 'Masha'},
        {name: 'Vasya'}
    ]
};

const sideBarReducer = (state = initialState, action) => {

    return state;
}